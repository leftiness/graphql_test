type Query {
  user(id: Int!): User
  users: [User]
}

type Mutation {
  insertUser(name: String!): [User]
  updateUser(id: Int!, name: String!): [User]
  deleteUser(id: Int!): [User]
}

type User {
  id: Int!
  name: String!
}